<!DOCTYPE html>
<html>

    <head>
        <meta charset="utf-8">
        <title>Lista de tareas</title>
        <link rel="stylesheet" href="css/styles.css">
    </head>

    <body>
        <div class="box" id="heading">
            <h1><%= day %></h1>
        </div>

        <div class="box">
            <% for (var i=0; i<items.length; i++) { %>
                <div class="item">
                    <input type="checkbox">
                    <p><%= items[i] %></p>
                </div>
            <% } %>
        
            <form class="item" action="/" method="post">
                <input type="text" name="newItem" placeholder="Agregar tarea">
                <button type="submit">+</button>
            </form>
        </div>
    </body>

</html>